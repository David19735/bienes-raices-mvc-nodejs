extends ../layout/index
block contenido
     div(class="py-2")
        h1(class="text-4xl my-5 font-extrabold text-center") Bienes
            span(class="font-normal") Raíces
        h2(class="text-center text-2xl font-extrabold")=pagina
        if errores
            div(class="max-w-md mx-auto my-10")
                each error in errores
                    p(class="bg-red-500 text-white uppercase text-xs text-center mb-1 font-bold py-2 rounded-md my-2")=error.msg
        div(class="mt-8 mx-auto max-w-md")
            div(class="bg-white py-8 px-4 shadow")
                form(method="POST")
                    input(type="hidden" name="_csrf" value=csrfToken)   
                    div(class="mb-6") 
                        label(class="block text-sm uppercase text-gray-500 mb-3 font-bold" for="password") Escribe tu nueva contraseña
                        input(class="w-full px-4 py-2 border border-gray-300 outline-none rounded-md placeholder-gray-500")(placeholder="Password" id="password" type="password" name="password")
                      
                    input(type="submit" value="Guaradar contraseña" class="w-full bg-indigo-600 hover:bg-indigo-800 p-2 rounded-sm text-white font-bold cursor-pointer")  