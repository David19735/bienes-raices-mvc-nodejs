extends ../layout/index
block contenido
     div(class="py-2")
        h1(class="text-4xl my-5 font-extrabold text-center") Bienes
            span(class="font-normal") Raíces
        h2(class="text-center text-2xl font-extrabold")=pagina
     div(class="bg-white shadow py-8 px-5 rounded-lg max-w-md mx-auto")
        p(class=error ? 'bg-red-500' : 'bg-green-500').py-2.px-5.rounded-lg.w-full.text-white.text-center.font-bold.uppercase.mx-auto=mensaje

        if !error
            a(href="/auth/login" class="text-center font-bold text-md text-slate-600 uppercase mt-10 block hover:text-blue-600") Inicia Sesión
