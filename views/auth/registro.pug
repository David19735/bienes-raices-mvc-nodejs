extends ../layout/index
block contenido
    div(class="py-2")
        h1(class="text-4xl my-5 font-extrabold text-center") Bienes
            span(class="font-normal") Raíces
        h2(class="text-center text-2xl font-extrabold")=pagina

        if errores
            div(class="max-w-md mx-auto my-10")
                each error in errores
                    p(class="bg-red-500 text-white uppercase text-xs text-center mb-1 font-bold py-2 rounded-md my-2")=error.msg

        div(class="mt-8 mx-auto max-w-md")
            div(class="bg-white py-8 px-4 shadow")
                form(method="POST" action="/auth/registro")
                    input(type="hidden" name="_csrf" value=csrfToken)
                    div(class="mb-6") 
                        label(class="block text-sm uppercase text-gray-500 mb-3 font-bold") Tu nombre
                        input(class="w-full px-4 py-2 border border-gray-300 outline-none rounded-md placeholder-gray-500")(placeholder="Nombre" for="nombre" id="nombre" type="text" name="nombre" value=usuario ? usuario.nombre: '')
                    div(class="mb-6") 
                        label(class="block text-sm uppercase text-gray-500 mb-3 font-bold") Email de registro
                        input(class="w-full px-4 py-2 border border-gray-300 outline-none rounded-md placeholder-gray-500")(placeholder="Tu Email" for="email" id="email" type="email" name="email" value= usuario ? usuario.email:'')
                    div(class="mb-6") 
                        label(class="block text-sm uppercase text-gray-500 mb-3 font-bold") Password
                        input(class="w-full px-4 py-2 border border-gray-300 outline-none rounded-md placeholder-gray-500")(placeholder="Ingresa la contraseña" for="password" id="password" type="password" name="password")    
                    div(class="mb-6") 
                        label(class="block text-sm uppercase text-gray-500 mb-3 font-bold") Repetir Password
                        input(class="w-full px-4 py-2 border border-gray-300 outline-none rounded-md placeholder-gray-500")(placeholder="Repite la contraseña" for="repetir_password" id="repetir_password" type="password" name="repetir_password")  
                    div(class="flex items-center justify-between")
                        a(href="/auth/login")(class="font-light text-gray-700 mb-4 text-xs hover:text-blue-400") ¿Ya tienes cuenta? Inicia Sesión
                        a(href="/auth/olvide-password")(class="font-light text-gray-700 mb-4 text-xs hover:text-blue-400") Olvidé mi contraseña   

                    input(type="submit" value="Crear Cuenta" class="w-full bg-indigo-600 hover:bg-indigo-800 p-2 rounded-sm text-white font-bold cursor-pointer")    